# Universo Platformo Next

**Полностековая реализация Universo Platformo / Universo MMOOMM на Next.js 15**

[![License](https://img.shields.io/badge/license-MIT-blue.svg)](LICENSE)
[![TypeScript](https://img.shields.io/badge/TypeScript-5.x-blue)](https://www.typescriptlang.org/)
[![Next.js](https://img.shields.io/badge/Next.js-15.x-black)](https://nextjs.org/)
[![PNPM](https://img.shields.io/badge/PNPM-8.x-orange)](https://pnpm.io/)

## Обзор

Universo Platformo Next — это модульная полностековая платформа на Next.js 15 с TypeScript. Проект реализует архитектурные концепции [Universo Platformo React](https://github.com/teknokomo/universo-platformo-react) с использованием лучших практик Next.js App Router и строгой структуры монорепозитория PNPM.

Проект предоставляет **гостевую стартовую страницу**, **авторизованный дашборд** и полный **поток аутентификации через Supabase** — при этом фронтенд взаимодействует исключительно через собственный бэкенд-API, никогда напрямую с Supabase.

## 🏗️ Архитектура

### Принцип безопасности: Supabase только через бэкенд

**Фронтенд никогда не обращается к Supabase напрямую.** Все операции аутентификации и работы с данными проходят через собственные Next.js API-маршруты (бэкенд). Токены Supabase хранятся в HttpOnly-куки, управляемых сервером.

```
Браузер (фронтенд)
    │  fetch /api/v1/auth/login   (credentials: same-origin)
    ▼
Next.js API-маршруты (бэкенд)
    │  supabase.auth.signInWithPassword(...)
    ▼
Supabase (внешний сервис)
```

### Структура проекта

```
universo-platformo-next/
├── apps/
│   └── web/                        # Приложение Next.js 15
│       ├── src/
│       │   ├── app/
│       │   │   ├── api/v1/auth/    # Бэкенд: login, logout, me, register
│       │   │   ├── auth/           # Маршрут страницы аутентификации
│       │   │   └── layout.tsx      # Корневой лэйаут с провайдерами
│       │   ├── lib/supabase/       # Серверный Supabase-клиент
│       │   └── providers/          # Провайдеры уровня приложения
│       └── middleware.ts           # Middleware обновления сессии
├── packages/
│   ├── auth-frt/base/              # Контекст авторизации, хуки, форма входа
│   └── start-frt/base/             # Стартовые страницы (гость + авторизован)
├── .env.example                    # Необходимые переменные окружения
├── package.json                    # Корень монорепозитория
├── pnpm-workspace.yaml
└── turbo.json
```

## 🚀 Быстрый старт

### Предварительные требования

- **Node.js**: 20+
- **PNPM**: 8.x или выше
- **Supabase**: Проект с включённой аутентификацией по email/паролю

### Настройка окружения

Скопируйте `.env.example` в `.env.local` и заполните учётные данные Supabase:

```bash
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

### Установка

```bash
# Клонировать репозиторий
git clone https://github.com/teknokomo/universo-platformo-next.git
cd universo-platformo-next

# Установить зависимости
pnpm install

# Запустить сервер разработки
pnpm dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## 📦 Технологический стек

- **Фреймворк**: Next.js 15.x (App Router)
- **Язык**: TypeScript 5.x (строгий режим)
- **Менеджер пакетов**: PNPM 8.x (рабочие пространства)
- **Система сборки**: Turborepo
- **База данных / Auth**: Supabase (только на сервере, через `@supabase/ssr`)
- **UI-фреймворк**: Material UI v6 с Emotion
- **Управление состоянием**: React Context + хуки (без внешних библиотек)
- **Стилизация**: MUI system + `sx`-проп (без дополнительных CSS-файлов)

## 🔑 Архитектура аутентификации

Аутентификация реализована так, что:

1. **Фронтенд** (`packages/auth-frt`) обращается только к маршрутам `/api/v1/auth/*` — без зависимостей от Supabase.
2. **Бэкенд** — API-маршруты — использует серверный клиент `@supabase/ssr` для работы с Supabase.
3. Токены сессии хранятся в **HttpOnly-куки**, установленных бэкендом и недоступных для JavaScript.
4. **Middleware** (`apps/web/middleware.ts`) автоматически обновляет истёкшие сессии при каждом запросе.

### API-маршруты

| Метод | Маршрут | Описание |
|-------|---------|----------|
| POST | `/api/v1/auth/login` | Вход по email и паролю |
| POST | `/api/v1/auth/register` | Создание нового аккаунта |
| POST | `/api/v1/auth/logout` | Выход и очистка сессии |
| GET | `/api/v1/auth/me` | Получение текущего авторизованного пользователя |

## 🧪 Разработка

### Доступные скрипты

```bash
# Разработка
pnpm dev              # Запустить сервер разработки (apps/web на порту 3000)
pnpm build            # Сборка для продакшена
pnpm type-check       # Проверка типов TypeScript во всех пакетах

# Команды рабочего пространства
pnpm --filter @universo/web dev     # Запустить dev в конкретном пакете
pnpm --filter @universo/web build   # Собрать конкретный пакет
```

## 🤝 Вклад в проект

1. **Прочитайте Конституцию**: Изучите [constitution.md](.specify/memory/constitution.md)
2. **Создайте Issue**: Следуйте [github-issues.md](.github/instructions/github-issues.md)
3. **Создайте ветку**: Следуйте подходу «спецификация первым»
4. **Реализуйте в пакетах**: Весь код функций должен находиться в `packages/`
5. **Напишите тесты**: Поддерживайте достаточное покрытие тестами
6. **Обновите документацию**: И на английском (`README.md`), и на русском (`README-RU.md`)
7. **Создайте PR**: Следуйте [github-pr.md](.github/instructions/github-pr.md)

## 📋 Статус проекта

**Текущая фаза**: Фаза 1 — Фундамент и аутентификация

- ✅ Структура монорепозитория (PNPM workspaces + Turborepo)
- ✅ Приложение Next.js 15 App Router (`apps/web`)
- ✅ Гостевая стартовая страница с hero-секцией и карточками продуктов
- ✅ Авторизованный дашборд с возможностью выхода
- ✅ Supabase-аутентификация через бэкенд API-маршруты (login, register, logout, me)
- ✅ Управление сессией через HttpOnly-куки и автообновление в middleware
- 🚧 Дополнительные пакеты функций (Kiberplano, Kompendio и др.)

## 📄 Лицензия

[Лицензия MIT](LICENSE)

## 🔗 Ссылки

- [Universo Platformo React](https://github.com/teknokomo/universo-platformo-react) — Эталонная реализация
- [Документация проекта](https://github.com/teknokomo/universo-platformo-next/wiki) — Подробные руководства
- [Трекер задач](https://github.com/teknokomo/universo-platformo-next/issues) — Сообщить об ошибках или запросить функции

---

**⚠️ Помните**: Фронтенд НИКОГДА не должен обращаться к Supabase напрямую — всегда через `/api/v1/`.
